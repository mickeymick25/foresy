---
openapi: 3.0.1
info:
  title: API Foresy
  version: v1
  description: Documentation de l'API Foresy
paths:
  "/api/v1/auth/login":
    post:
      summary: Authenticates a user
      tags:
      - Authentication
      parameters: []
      responses:
        '200':
          description: user authenticated
        '401':
          description: missing email
        '403':
          description: inactive user
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
              required:
              - email
              - password
  "/api/v1/auth/logout":
    delete:
      summary: Logs out
      tags:
      - Authentication Logout
      parameters:
      - name: Authorization
        in: header
        required: false
        schema:
          type: string
      responses:
        '200':
          description: Logout successful
        '401':
          description: logout fails with expired token
        '500':
          description: internal server error
  "/api/v1/oauth/google_oauth2/callback":
    get:
      summary: OAuth callback for google_oauth2
      tags:
      - OAuth
      responses:
        '200':
          description: successful OAuth login
        '401':
          description: missing OAuth data
        '422':
          description: user creation failed
    post:
      summary: Logs in via google_oauth2
      tags:
      - Authentication via Google Oauth2
      responses:
        '200':
          description: OAuth login successful
  "/api/v1/oauth/github/callback":
    get:
      summary: OAuth callback for github
      tags:
      - OAuth
      responses:
        '200':
          description: successful OAuth login
        '401':
          description: missing OAuth data
        '422':
          description: user creation failed
    post:
      summary: Logs in via github
      tags:
      - Authentication via Github
      responses:
        '200':
          description: OAuth login successful
  "/api/v1/auth/refresh":
    post:
      summary: Refreshes authentication token
      tags:
      - Authentication
      parameters: []
      responses:
        '200':
          description: token refreshed
        '401':
          description: refresh token expired
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                refresh_token:
                  type: string
              required:
              - refresh_token
  "/api/v1/auth/{provider}/callback":
    post:
      summary: OAuth callback for provider authentication
      tags:
      - OAuth
      parameters:
      - name: provider
        in: path
        type: string
        required: true
        description: OAuth provider
        schema:
          type: string
          enum:
          - google_oauth2
          - github
      responses:
        '200':
          description: successful OAuth authentication
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    description: JWT authentication token
                  user:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                        description: User unique identifier
                      email:
                        type: string
                        format: email
                        description: User email address
                      provider:
                        type: string
                        description: OAuth provider used
                      provider_uid:
                        type: string
                        description: User unique identifier from OAuth provider
                    required:
                    - id
                    - email
                    - provider
                    - provider_uid
                required:
                - token
                - user
        '400':
          description: invalid provider
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: invalid_provider
                    description: Error code
                required:
                - error
        '401':
          description: OAuth authentication failed
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: oauth_failed
                    description: Error code
                required:
                - error
        '422':
          description: invalid payload
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: invalid_payload
                    description: Error code
                required:
                - error
        '500':
          description: internal server error
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: internal_error
                    description: Error code
                required:
                - error
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  description: OAuth authorization code
                redirect_uri:
                  type: string
                  format: uri
                  description: Redirect URI used in OAuth flow
              required:
              - code
              - redirect_uri
        required: true
        description: OAuth authorization code received from provider
  "/api/v1/auth/failure":
    get:
      summary: OAuth failure endpoint
      tags:
      - OAuth
      responses:
        '401':
          description: OAuth authentication failed
          content:
            application/json:
              schema:
                type: object
                properties:
                  error:
                    type: string
                    example: oauth_failed
                    description: Error code
                  message:
                    type: string
                    example: OAuth authentication failed
                    description: Error message
                required:
                - error
                - message
  "/api/v1/signup":
    post:
      summary: Crée un nouvel utilisateur
      tags:
      - Users
      parameters: []
      responses:
        '201':
          description: Utilisateur créé
        '422':
          description: Création échouée
      requestBody:
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/user"
        required: true
servers:
- url: http://{defaultHost}
  description: Serveur local de développement
  variables:
    defaultHost:
      default: localhost:3000
components:
  securitySchemes:
    bearer_auth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
    user:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
        password_confirmation:
          type: string
          format: password
      required:
      - email
      - password
      - password_confirmation
    login:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
      required:
      - email
      - password
security:
- bearer_auth: []
